#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @t: integer to be used
 * Return: absolute value of integer
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
